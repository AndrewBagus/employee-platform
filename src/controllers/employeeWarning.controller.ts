import { createController } from "./base.controller";
import { TYPES } from "@cores/types";
export default createController(TYPES.EmployeeWarningServiceInterface, "employeeWarning", "employeeWarnings");
