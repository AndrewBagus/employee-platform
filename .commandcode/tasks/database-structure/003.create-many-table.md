# 003 — Create All Master & Transaction Tables

**Plan reference:** `.commandcode/plans/database-structure/003.create-many-table.md`

## Tasks

### 1. Master Tables (no FKs to employee)

Each table follows the same pattern:
- UUIDv7 PK (`id`), spread `...defaultColumn` at end
- Files go in `src/db/schema/`

| # | File | Table | Columns (beyond id + defaultColumn) | Imports |
|---|------|-------|-------------------------------------|---------|
| 1 | `religion.ts` | religion | `name` varchar(20), `description` varchar(255), `remark` text | uuid, varchar, pgTable, text |
| 2 | `costCenters.ts` | costCenters | `sapCode` varchar(15), `name` varchar(255), `remark` text | uuid, varchar, pgTable, text |
| 3 | `grades.ts` | grades | `name` varchar(15), `remark` text | uuid, varchar, pgTable, text |
| 4 | `projects.ts` | projects | `sapCode` varchar(15), `name` varchar(255), `remark` text | uuid, varchar, pgTable, text |
| 5 | `allowances.ts` | allowances | `companyId` uuid FK→companies, `currencyId` uuid FK→countries, `name` varchar(255), `nominal` numeric(12,2), `remark` text | uuid, varchar, pgTable, numeric, text, + FK imports |
| 6 | `warningGrades.ts` | warningGrades | `companyId` uuid FK→companies, `name` varchar(255), `remark` text | uuid, varchar, pgTable, text, + FK imports |
| 7 | `warnings.ts` | warnings | `companyId` uuid FK→companies, `warningGradeId` uuid FK→warningGrades, `name` varchar(255), `remark` text | uuid, varchar, pgTable, text, + FK imports |
| 8 | `trainingTypes.ts` | trainingTypes | `companyId` uuid FK→companies, `name` varchar(255), `remark` text | uuid, varchar, pgTable, text, + FK imports |
| 9 | `trainings.ts` | trainings | `companyId` uuid FK→companies, `name` varchar(255), `remark` text | uuid, varchar, pgTable, text, + FK imports |
| 10 | `trainingSchedules.ts` | trainingSchedules | `companyId` uuid FK→companies, `trainingDate` date nullable, `trainingTime` time nullable, `remark` text | uuid, pgTable, date, time, text, + FK imports |
| 11 | `workingZones.ts` | workingZones | `companyId` uuid FK→companies, `name` varchar(255), `remark` text | uuid, varchar, pgTable, text, + FK imports |

### 2. Employee Master Table

**File:** `src/db/schema/employees.ts`

Columns (beyond id + defaultColumn):
- `companyId` uuid FK→companies (restrict)
- `departmentId` uuid FK→departments (restrict)
- `positionId` uuid FK→positions (restrict)
- `supervisorId` uuid self-referencing FK→employees.id (nullable) — use `type AnyPgColumn` pattern
- `fingerId` varchar(30)
- `firstName` varchar(255)
- `middleName` varchar(255) nullable
- `lastName` varchar(255) nullable
- `email` varchar(255)
- `phone` varchar(255) nullable
- `mobile` varchar(255) nullable
- `birthDate` date nullable
- `birthPlace` varchar(255) nullable
- `gender` enum ["M","W"] default "M"
- `maritalStatus` enum ["SINGLE","MARRIED","DIVORCED"] default "SINGLE"
- `employeeType` enum ["STAFF","WORKER"] default "STAFF"
- `employeeStatus` enum ["PERMANENT","CONTRACT"] default "CONTRACT"
- `remark` text

**4 pgEnums needed:** `gender`, `maritalStatus`, `employeeType`, `employeeStatus`

### 3. Employee Transaction Tables

| # | File | Table | Columns (beyond id + defaultColumn) | Special |
|---|------|-------|-------------------------------------|---------|
| 12 | `employeeAddresses.ts` | employeeAddresses | `employeeId` uuid FK→employees, `address` text, `remark` text | — |
| 13 | `employeeContracts.ts` | employeeContracts | `employeeId` uuid FK→employees, `startDate` date, `endDate` date, `terminationDate` date nullable, `remark` text | — |
| 14 | `employeeContractAdvisors.ts` | employeeContractAdvisors | `employeeId` uuid FK→employees, `order` smallint, `remark` text | — |
| 15 | `employeeLeaveAdvisors.ts` | employeeLeaveAdvisors | `employeeId` uuid FK→employees, `order` smallint, `remark` text | — |
| 16 | `employeeProjects.ts` | employeeProjects | `employeeId` uuid FK→employees (PK), `projectId` uuid FK→projects (PK), `remark` text | **Composite PK**: `employeeId` + `projectId`, no separate `id` column |
| 17 | `employeeMcus.ts` | employeeMcus | `employeeId` uuid FK→employees, `mcuDate` date, `mcuEndDate` date nullable, `mcuStatus` enum ["PASS","FAILED"], `remark` text | pgEnum: `mcuStatus` |
| 18 | `employeeWarnings.ts` | employeeWarnings | `employeeId` uuid FK→employees, `warningStartDate` date, `warningEndDate` date nullable, `warningStatus` enum ["PERMANENT","TEMPORARY"], `remark` text | pgEnum: `warningStatus` |
| 19 | `employeeTrainings.ts` | employeeTrainings | `employeeId` uuid FK→employees, `trainingDate` date, `trainingStatus` enum ["PASS","FAILED","REGISTERED","NOTATTEND"] default "REGISTERED", `remark` text | pgEnum: `trainingStatus` |
| 20 | `employeeWorkZones.ts` | employeeWorkZones | `employeeId` uuid FK→employees, `workingZoneId` uuid FK→workingZones, `remark` text | — |

### 4. Drizzle Import Summary

New types from `drizzle-orm/pg-core`:
- `text`, `numeric`, `smallint`, `date`, `time`, `primaryKey` (for composite PK)
- `pgEnum` — for 7 enums total (gender, maritalStatus, employeeType, employeeStatus, mcuStatus, warningStatus, trainingStatus)
- `type AnyPgColumn` — for self-referencing FK (`supervisorId`)

### 5. Composite PK Pattern (employeeProjects)

```ts
// No standalone id column, use composite PK
import { primaryKey } from "drizzle-orm/pg-core";

export const employeeProjects = pgTable("employee_projects", {
  employeeId: uuid().references(() => employees.id, { onDelete: "restrict" }),
  projectId: uuid().references(() => projects.id, { onDelete: "restrict" }),
  remark: text(),
  ...defaultColumn,
}, (table) => ({
  pk: primaryKey({ columns: [table.employeeId, table.projectId] }),
}));
```

### 6. Verify
- Run `bunx --bun tsc --noEmit` (expect only pre-existing node_modules drizzle errors)
- Run `bun run generate -- --name all-master-transaction`

### Dependencies
- All 4 existing schema tables must exist: countries, companies, departments, positions
- `employeeProjects` needs both `employees` and `projects` created first (import order: employees → projects in same migration)
- `warnings` imports `warningGrades` — warningGrades must be created first
