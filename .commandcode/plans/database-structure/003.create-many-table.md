## Main Object

- create table religion
- create table costCenter
- create table grade
- create table project
- create table allowance
- create table warningGrade
- create table warning
- create table trainingType
- create table training
- create table trainingSchedule
- create table workingZone
- create table employee
- create table employeeAddress
- create table employeeContract
- create table employeeContractAdvisor
- create table employeeLeaveAdvisor
- create table employeeProject
- create table employeeMcu
- create table employeeWarning
- create table employeeTraining
- create table employeeWorkZone

## Step Instruction

### Table Religion

1. create new file religion.ts
2. the columns :
   a. id reference existing table
   b. name varchar 20
   c. description varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Cost Center

1. create new file costCenters.ts
2. the columns :
   a. id reference existing table
   b. sapCode varchar 15
   c. name varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Grade

1. create new file grades.ts
2. the columns :
   a. id reference existing table
   b. name varchar 15
   c. remark text
   d. rest of column follow is defaultColumn

### Table Project

1. create new file projects.ts
2. the columns :
   a. id reference existing table
   b. sapCode varchar 15
   c. name varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Allowance

1. create new file allowances.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. currencyId uuid reference with country.id
   d. name varchar 255
   e. nominal numeric 12,2
   f. remark text
   g. rest of column follow is defaultColumn

### Table Warning Grade

1. create new file warningGrades.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. name varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Warning

1. create new file warnings.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. warningGradeId uuid reference with warningGrade.id
   d. name varchar 255
   e. remark text
   f. rest of column follow is defaultColumn

### Table Training Type

1. create new file trainingTypes.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. name varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Training

1. create new file trainings.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. name varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Training Schedule

1. create new file trainingSchedules.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. trainingDate date allow null
   d. trainingTime time allow null
   e. remark text
   f. rest of column follow is defaultColumn

### Table Working Zone

1. create new file workingZones.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. name varchar 255
   d. remark text
   e. rest of column follow is defaultColumn

### Table Employee

1. create new file employees.ts
2. the columns :
   a. id reference existing table
   b. companyId uuid reference with company.id
   c. departmentId uuid reference with department.id
   d. positionId uuid reference with position.id
   e. supervisorId uuid recursive with this id
   f. fingerid varchar 30
   g. firstName varchar 255
   h. middleName varchar 255 allow null
   i. lastName varchar 255 allow null
   j. email varchar 255
   k. phone varchar 255 allow null
   l. mobile varchar 255 allow null
   m. birthDate date allow null
   n. birthPlace varchar 255 allow null
   o. gender enum ["M", "W"] default "M"
   p. maritalStatus enum ["SINGLE", "MARRIED", "DIVORCED"] default "SINGLE"
   q. employeeType enum ["STAFF", "WORKER"] default "STAFF"
   r. employeeStatus enum ["PERMANENT", "CONTRACT"] default "CONTRACT"
   s. remark text
   t. rest of column follow is defaultColumn

### Table Employee Address

1. create new file employeeAddresses.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. address text
   d. remark text
   e. rest of column follow is defaultColumn

### Table Employee Contract

1. create new file employeeContracts.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. startDate date
   d. endDate date
   e. termination date
   f. remark text
   g. rest of column follow is defaultColumn

### Table Employee Contract Advisor

1. create new file employeeContractAdvisors.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. order smallInt
   d. remark text
   e. rest of column follow is defaultColumn

### Table Employee Leave Advisor

1. create new file employeeLeaveAdvisors.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. order smallInt
   d. remark text
   e. rest of column follow is defaultColumn

### Table Employee Project

1. create new file employeeProjects.ts
2. the columns :
   a. employeeId uuid reference with employee.id as primary key
   b. projectId uuid reference with project.id as primary key
   c. remark text
   d. rest of column follow is defaultColumn

### Table Employee MCU

1. create new file employeeMcus.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. mcuDate date
   d. mcuEndDate date allow null
   e. mcuStatus enum ["PASS", "FAILED"]
   f. remark text
   g. rest of column follow is defaultColumn

### Table Employee Warning

1. create new file employeeWarnings.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. warningStartDate date
   d. warningEndDate date allow null
   e. warningStatus enum ["PERMANENT", "TEMPORARY"]
   f. remark text
   g. rest of column follow is defaultColumn

### Table Employee Training

1. create new file employeeTrainings.ts
2. the columns :
   a. id reference existing table
   b. employeeId uuid reference with employee.id
   c. trainingDate date
   e. trainingStatus enum ["PASS", "FAILED", "REGISTERED", "NOTATTEND"] default "REGISTERED"
   f. remark text
   g. rest of column follow is defaultColumn
